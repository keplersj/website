require 'middleman-gh-pages'

# EXPERIMENTAL: Push to GitHub Pages
# FIXME
#Rake::Task[:publish].enhance do
#  Rake::Task[:extra_behavior].invoke
#end
#
#task :push_to_correct_branch do
#  Dir.chdir 'build'
#  system 'git push origin gh-pages:master'
#  Dir.chdir '..'
#end